{
  "name": "admin",
  "private": true,
  "type": "module",
  "author": "NeoXam",
  "scripts": {
    "lint": "npx eslint . --fix",
    "licenses:admin": "node scripts/manage/license-checks.js . admin",
    "licenses:backend": "node scripts/manage/license-checks.js ../backend backend",
    "licenses:frontend": "node scripts/manage/license-checks.js ../frontend frontend",
    "licenses:proxy": "node scripts/manage/license-checks.js ../proxy proxy",
    "licenses:all": "npm run licenses:admin && npm run licenses:backend && npm run licenses:frontend && npm run licenses:proxy",
    "merge-scans": "node scripts/manage/merge-vulnerability-scans.js",
    "merge-flaws": "node scripts/manage/merge-flaw-scans.js"
  },
  "engines": {
    "node": ">=22.13.x"
  },
  "dependencies": {
    "console-table-printer": "^2.15.0",
    "dotenv": "^16.3.1",
    "license-checker-rseidelsohn": "^4.4.2"
  },
  "overrides": {
    "glob": "^11.0.3",
    "minimatch": "^10.2.2"
  }
}
